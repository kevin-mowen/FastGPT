# FastGPT Docker 环境变量配置示例
# 复制此文件为 .env 并修改相关配置

# ⚠️ 安全警告：生产环境必须修改所有默认密码！

# MongoDB 配置
MONGO_USERNAME=myusername
MONGO_PASSWORD=强密码请修改_change_this_password

# Redis 配置
REDIS_PASSWORD=强密码请修改_change_this_password

# MinIO 配置
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=强密码请修改_change_this_password

# PostgreSQL (用于 aiproxy) 配置
PG_USER=postgres
PG_DB=aiproxy
PG_PASSWORD=强密码请修改_change_this_password

# AI Proxy 管理密钥
AIPROXY_ADMIN_KEY=强密钥请修改_change_this_key

# 镜像源配置 (可选)
# 如果无法访问 Docker Hub，可以使用阿里云镜像
# DOCKER_REGISTRY_PREFIX=registry.cn-hangzhou.aliyuncs.com/

# 说明：
# 1. 所有包含 "change_this" 的值都必须修改
# 2. 密码建议使用至少12位的随机字符串
# 3. 生产环境不要暴露不必要的端口
# 4. 定期更换密码和密钥